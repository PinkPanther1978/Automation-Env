# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: example.spec.ts >> Verify search functionality on Playwright website
- Location: tests/example.spec.ts:3:5

# Error details

```
Error: expect(locator).toContainText(expected) failed

Locator: locator('header h1')
Expected substring: "Trace Viewer"
Received string:    "Playwright enables reliable web automation for testing, scripting, and AI agents."
Timeout: 5000ms

Call log:
  - Expect "toContainText" with timeout 5000ms
  - waiting for locator('header h1')
    14 × locator resolved to <h1 class="hero__title heroTitle_ohkl">…</h1>
       - unexpected value "Playwright enables reliable web automation for testing, scripting, and AI agents."

```

```yaml
- heading "Playwright enables reliable web automation for testing, scripting, and AI agents." [level=1]
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test('Verify search functionality on Playwright website', async ({ page }) => {
  4  |   // 1. Navigate to the website
  5  |   await page.goto('https://playwright.dev');
  6  | 
  7  |   // 2. Click the Search button to open the modal
  8  |   const searchButton = page.getByLabel('Search');
  9  |   await searchButton.click();
  10 | 
  11 |   // 3. Type "Trace Viewer" into the search input field
  12 |   const searchInput = page.getByPlaceholder('Search docs');
  13 |   await searchInput.fill('Trace Viewer');
  14 | 
  15 |   // 4. Press Enter and verify the page header updates correctly
  16 |   await searchInput.press('Enter');
  17 |   const mainHeader = page.locator('header h1');
> 18 |   await expect(mainHeader).toContainText('Trace Viewer');
     |                            ^ Error: expect(locator).toContainText(expected) failed
  19 | 
  20 |   // 5. Automatically capture a screenshot and save it
  21 |   await page.screenshot({ path: 'allure-results/success-screenshot.png', fullPage: true });
  22 | });
  23 | 
```